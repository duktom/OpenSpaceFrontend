meta {
  name: Login | User
  type: http
  seq: 1
}

post {
  url: {{BASE_URL}}/account/login
  body: json
  auth: inherit
}

body:json {
  {
      "email": "user@gmail.com",
      "password": "password"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
